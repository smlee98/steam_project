<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="register">
	<insert id="insertOne" parameterType="com.example.demo.dto.RegisterDTO">
		insert into tb_user
		values(#{number},#{id},#{password},#{name},#{gender},#{address},#{phone},#{favorite})
	</insert>
</mapper>